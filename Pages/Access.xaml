<Page x:Class="DataGateWin.Pages.Access"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:utils="clr-namespace:DataGateWin.ViewModels.Utils">

    <Page.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <utils:BytesToHumanReadableConverter x:Key="BytesFmt"/>
        <utils:PlanAccessYesNoConverter x:Key="PlanAccessYesNo"/>
    </Page.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header + Refresh -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <ui:TextBlock Grid.Column="0"
                          Text="Access"
                          FontTypography="Title"
                          FontSize="20"
                          Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                          FontWeight="SemiBold"/>

            <ui:Button Grid.Column="1"
                       Height="34"
                       MinWidth="110"
                       Appearance="Secondary"
                       Command="{Binding RefreshCommand}">
                <ui:Button.Icon>
                    <ui:SymbolIcon Symbol="ArrowClockwise24"/>
                </ui:Button.Icon>
                <ui:Button.Content>
                    <TextBlock Text="Refresh"/>
                </ui:Button.Content>
            </ui:Button>
        </Grid>

        <!-- Plan & quotas (Linux MainWindow Access card) -->
        <ui:Card Grid.Row="1" Margin="0,16,0,0">
            <StackPanel Margin="4">
                <TextBlock Text="Your plan &amp; quotas"
                           FontWeight="SemiBold"
                           FontSize="14"/>
                <TextBlock Text="{Binding PlanLineText}"
                           Margin="0,8,0,0"
                           TextWrapping="Wrap"
                           Opacity="0.85"/>
                <TextBlock Text="Traffic quota"
                           Margin="0,12,0,0"
                           FontWeight="SemiBold"
                           Visibility="{Binding ShowTrafficQuotaTitle, Converter={StaticResource BoolToVis}}"/>
                <TextBlock Text="{Binding QuotaMetaText}"
                           Margin="0,6,0,0"
                           TextWrapping="Wrap"
                           Opacity="0.85"
                           Visibility="{Binding QuotaMetaVisible, Converter={StaticResource BoolToVis}}"/>
                <ProgressBar Height="12"
                             Margin="0,6,0,0"
                             Minimum="0"
                             Maximum="100"
                             Value="{Binding QuotaBarValue}"
                             Visibility="{Binding QuotaBarVisible, Converter={StaticResource BoolToVis}}">
                    <ProgressBar.Style>
                        <Style TargetType="ProgressBar">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding QuotaBarIsOver}" Value="True">
                                    <Setter Property="Foreground" Value="#E81123"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </ProgressBar.Style>
                </ProgressBar>
                <TextBlock Text="{Binding QuotaDetailsText}"
                           Margin="0,6,0,0"
                           TextWrapping="Wrap"
                           Opacity="0.85"
                           Visibility="{Binding QuotaDetailsVisible, Converter={StaticResource BoolToVis}}"/>
                <TextBlock Text="{Binding ValidityFooterText}"
                           Margin="0,10,0,0"
                           TextWrapping="Wrap"
                           Opacity="0.85"/>
            </StackPanel>
        </ui:Card>

        <!-- Loading / Error -->
        <Grid Grid.Row="2" Margin="0,12,0,12">
            <ui:ProgressRing
                IsIndeterminate="True"
                Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}"/>

            <TextBlock
                Text="{Binding ErrorText}"
                Foreground="Red">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Visibility" Value="Visible"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding ErrorText}" Value="{x:Null}">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding ErrorText}" Value="">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
        </Grid>

        <!-- Servers (scroll here when window is small) -->
        <ScrollViewer Grid.Row="3"
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled">

            <ui:DataGrid
                ItemsSource="{Binding Servers}"
                AutoGenerateColumns="False"
                IsReadOnly="True"
                CanUserAddRows="False"
                CanUserDeleteRows="False"
                CanUserResizeRows="False"
                CanUserReorderColumns="False"
                CanUserSortColumns="True"
                SelectionMode="Single"
                SelectionUnit="FullRow"
                HeadersVisibility="Column"
                GridLinesVisibility="All"
                HorizontalGridLinesBrush="{DynamicResource ControlStrokeColorDefaultBrush}"
                VerticalGridLinesBrush="{DynamicResource ControlStrokeColorDefaultBrush}"
                BorderThickness="1"
                BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}"
                RowHeaderWidth="0">

                <ui:DataGrid.Columns>

                    <DataGridTextColumn
                        Header="Server"
                        Binding="{Binding OpenVpnServerResponses.OpenVpnServer.ServerName}"
                        Width="*" />

                    <DataGridTextColumn
                        Header="Clients"
                        Binding="{Binding CountConnectedClients}"
                        Width="90"/>

                    <DataGridTextColumn
                        Header="In"
                        Binding="{Binding TotalBytesIn, Converter={StaticResource BytesFmt}, ConverterParameter=IEC}"
                        Width="120"/>

                    <DataGridTextColumn
                        Header="Out"
                        Binding="{Binding TotalBytesOut, Converter={StaticResource BytesFmt}, ConverterParameter=IEC}"
                        Width="120"/>

                    <DataGridCheckBoxColumn
                        Header="Online"
                        Binding="{Binding OpenVpnServerResponses.OpenVpnServer.IsOnline}"
                        Width="90">
                        <DataGridCheckBoxColumn.ElementStyle>
                            <Style TargetType="CheckBox">
                                <Setter Property="IsEnabled" Value="False"/>
                                <Setter Property="HorizontalAlignment" Value="Center"/>
                            </Style>
                        </DataGridCheckBoxColumn.ElementStyle>
                    </DataGridCheckBoxColumn>

                    <DataGridTextColumn Header="Plan access" Width="100">
                        <DataGridTextColumn.Binding>
                            <Binding Converter="{StaticResource PlanAccessYesNo}"/>
                        </DataGridTextColumn.Binding>
                    </DataGridTextColumn>

                </ui:DataGrid.Columns>
            </ui:DataGrid>

        </ScrollViewer>

        <TextBlock Grid.Row="4"
                   Margin="0,8,0,0"
                   Text="{Binding TotalClientsLineText}"
                   Opacity="0.75"/>
    </Grid>
</Page>
